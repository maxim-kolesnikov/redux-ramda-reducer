{
  "name": "redux-ramda-reducer-example",
  "version": "0.1.0",
  "description": "Demo for createReducer with React + Redux + Ramda",
  "private": true,
  "dependencies": {
    "ramda": "^0.25.0",
    "react": "^16.6.0",
    "react-dom": "^16.6.0",
    "react-redux": "^5.1.0",
    "react-scripts": "2.0.5",
    "redux": "^4.0.1",
    "redux-ramda-reducer": "file:../"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "clean:deps": "rm -rf node_modules/ && rm yarn.lock"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}